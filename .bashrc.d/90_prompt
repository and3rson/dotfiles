#!/bin/bash

# Prevent PS1 from modifying PS1
# export VIRTUAL_ENV_DISABLE_PROMPT=1

# Using PROMPT_COMMAND instead of PS1 to hide virtualenv prefix
PROMPT_COMMAND="PS1=\$(RET=\$? $HOME/.bashrc.d/scripts/prompt.lua)"
# PS1="\$(RET=\$? $HOME/.bashrc.d/scripts/prompt.lua)"
